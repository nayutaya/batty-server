
<%-
  @rss_auto_discoveries = [
    {:title => "エネルギー履歴", :href => url_for(:controller => "user_feeds", :action => "energies", :user_token => @login_user.user_token)},
  ]
-%>

<div>
  <%= link_to(h("トップ"), root_path) %> &gt;
  <em>エネルギー履歴</em>
</div>

<div>
  <%- @energies.each_slice(10) { |energies| -%>
    <div style="float: left; margin-right: 10px;">
      <table border="1">
        <thead>
          <tr>
            <th>日時</th>
            <th colspan="2">デバイス</th>
            <th>エネ</th>
          </tr>
        </thead>
        <tbody>
          <%- energies.each { |energy| -%>
            <tr>
              <td><%=h yyyymmdd_hhmm(energy.observed_at) %></td>
              <td><%= device_icon24_link(energy.device) %></td>
              <td><%= device_name_link(energy.device) %></td>
              <td align="right"><%=h energy.observed_level %> %</td>
            </tr>
          <%- } -%>
        </tbody>
      </table>
    </div>
  <%- } -%>
  <div style="clear: left;"></div>
</div>

<div><%= will_paginate(@energies) %></div>
